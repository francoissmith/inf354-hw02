import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'u19314486_HW02',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
